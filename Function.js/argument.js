function multiplication(number){
    const result = number * 2;
    console.log(number, '--->', result);
}
multiplication(20);

// or 

function add (a){
    const total = a * 2;
    console.log(a, '-->', total);
}
const number = 120;
add(number);


// Or 
function age (father, mother){
    const difference = father - mother;
    console.log('Total age difference -->', difference);
}
const fatherAge = 40;
const motherAge = 35;
age(fatherAge, motherAge);